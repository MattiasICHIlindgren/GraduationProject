/*
#ifndef MENU_H
#define MENU_H
#include <TFT_eSPI.h>
class Menu
{
  public:
   void setup(TFT_eSPI*tft) = 0; // Pure virtual function to initialize the menu
   void handleInput(TFT_eSPI*tft) = 0; // Pure virtual function to handle user input
   void drawMenu(TFT_eSPI*tft) = 0; // Pure virtual function to draw the menu
};

#endif */
